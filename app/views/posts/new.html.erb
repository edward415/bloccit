<h1>New Post</h1>

<div class="row">
    <div class="col-md-4">
        <p>Guidlines for posts</p>
        <ul>
            <li>Make sure it rhymes.</li>
            <li>Use "A" 9 times.</li>
            <li>Hashtags = crime</li>
        </ul>
    </div>
    <div class="col-md-8">
        <%= form_for [@topic, @post] do |f| %>
        <div class="form-group">
            <%= f.label :title %>
            <%= f.text_field :title, class: 'form-control', placeholder: "Enter Post Title" %>
        </div>
        <div class="form-group">
            <%= f.label :body %>
            <%= f.text_area :body, rows: 8, class: 'form-control', placeholder: "Say what you want to say!" %>
        </div>
        <div class="form-group">
            <%= f.submit "Post!", class: 'btn btn-success' %>
        </div>
    <% end %>
    </div>
</div>

